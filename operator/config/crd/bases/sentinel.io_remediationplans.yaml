---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: remediationplans.sentinel.io
spec:
  group: sentinel.io
  names:
    kind: RemediationPlan
    listKind: RemediationPlanList
    plural: remediationplans
    singular: remediationplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: string
    - jsonPath: .spec.dryRun
      name: DryRun
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: RemediationPlan is the Schema for the remediationplans API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RemediationPlan
            properties:
              approvedBy:
                description: ApprovedBy records who approved the plan ("human" for
                  now).
                type: string
              dryRun:
                description: DryRun marks the plan as a preview — the operator must
                  NOT act.
                type: boolean
              incident:
                description: Incident is the raw incident/alert text that triggered
                  the plan.
                type: string
              planRef:
                description: PlanRef is the UUID of the persisted approval plan in
                  Postgres.
                type: string
              priority:
                description: Priority is one of "high", "medium", "low".
                type: string
              rationale:
                description: Rationale explains why this plan exists (tied to the
                  synthesis).
                type: string
              steps:
                description: Steps is the ordered list of remediation actions (1-3
                  steps).
                items:
                  description: RemediationStep is one concrete action in the plan.
                  properties:
                    action:
                      description: |-
                        Action is an allow-listed verb: restart, scale, rollback,
                        cordon, drain, patch, delete_pod, escalate.
                      type: string
                    detail:
                      description: Detail is a human-readable description of the change
                        + outcome.
                      type: string
                    target:
                      description: Target is what the action acts on, e.g. "deployment/demo-api".
                      type: string
                  required:
                  - action
                  - target
                  type: object
                maxItems: 5
                minItems: 1
                type: array
            required:
            - steps
            type: object
          status:
            description: status defines the observed state of RemediationPlan
            properties:
              conditions:
                description: Conditions represent the current state of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message is a human-readable explanation of the current
                  state.
                type: string
              observedGeneration:
                description: ObservedGeneration is the metadata.generation this status
                  reflects.
                format: int64
                type: integer
              state:
                description: |-
                  State is the lifecycle state driven by the reconcile loop (T3.9):
                  Proposed → Approved → Applied → Verified → Closed.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
